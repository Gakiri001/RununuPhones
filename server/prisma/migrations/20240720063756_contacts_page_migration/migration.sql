-- CreateTable
CREATE TABLE "subject_table" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "subject" TEXT NOT NULL,
    "explanation" TEXT NOT NULL,

    CONSTRAINT "subject_table_pkey" PRIMARY KEY ("id")
);
